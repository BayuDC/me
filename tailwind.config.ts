import type { Config } from 'tailwindcss';

export default {
    theme: {
        fontFamily: {
            sans: ['Poppins', 'sans-serif'],
        },
        colors: {
            transparent: 'transparent',
            primary: '#3DB2FF',
            primary2: '#0A9EFF',
            light: '#EFEFEF',
            dark: '#121212',
        },
    },
    content: [],
} as Config;
